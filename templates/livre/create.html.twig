{% extends 'base.html.twig' %}

{% block title %} Enregistrer un Livre{% endblock %}

{% block body %}

    <div class="container-sm mb-4">
        {{ form_start(livreForm) }}



        {{ form_widget(livreForm) }}


<div>
        <button class="btn btn-dark">Valider</button>
        <a class="btn btn-danger" href="{{ path('main_home') }}">Annuler</a>
</div>
        {{ form_end(livreForm) }}

    </div>



{% endblock %}
